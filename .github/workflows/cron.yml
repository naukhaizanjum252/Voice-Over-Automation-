name: Process Voiceovers

# Automatic schedule removed — processing now runs on the VPS worker (worker/index.ts).
# Kept as manual-only. Do NOT run it while the worker is active: it would trigger
# Vercel-side processing and double-process the same database.
on:
  workflow_dispatch:

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger voiceover processing
        run: |
          response=$(curl -s -w "\n%{http_code}" -X POST \
            "${APP_URL}/api/cron/process?secret=${CRON_SECRET}" \
            -H "Content-Type: application/json" \
            --max-time 540)
          http_code=$(echo "$response" | tail -1)
          body=$(echo "$response" | sed '$d')
          echo "Status: $http_code"
          echo "Response: $body"
          if [ "$http_code" -ge 400 ]; then
            echo "::error::Cron failed with status $http_code"
            exit 1
          fi
        env:
          APP_URL: ${{ secrets.APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
